在代码实现上，files和tags这两个文件读取和写入，应该有一层抽象，这两个文件也有可能之后会迁移到数据库吧。

一个通用部分。在分配，创建，搜索的时候都能用到的就是过滤标签，过滤标签的UI布局是最左列有一个可以上下滚动的。标签分组列表，右边是主视图区，也是一个可以上下滚动的，在这个区域内的目的是一行标签分组名称。然后是他下面的图片标签。接着是文本标签。然后就是下一行另一个分组名称。

这个快捷键有些是跟着页面显示与否来判断它的作用





移除toast的具体描述，仅说明 使用toast 给出提示就行。

和 前面的文件的内容对比，有遗漏和矛盾的以前面文件为准


